{
  "name": "clock-backend",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "npm --prefix ../frontend install && npm --prefix ../frontend run build && node src/server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "socket.io": "^4.7.2",
    "bcryptjs": "^3.0.0",
    "jsonwebtoken": "^9.0.0",
    "multer": "^2.0.1"
  }
}
